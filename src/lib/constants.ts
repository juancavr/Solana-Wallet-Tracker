/**
 * Shared constants — no server-only imports here.
 * Safe to use in both client and server components.
 */

/** Wrapped SOL mint address (used as the canonical SOL identifier in price maps) */
export const SOL_MINT = 'So11111111111111111111111111111111111111112';
